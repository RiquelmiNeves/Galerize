Documentação do Projeto: Galeria de Fotos Pessoal
Este documento detalha a arquitetura, funcionalidades e implementação do projeto de Galeria de Fotos Pessoal, desenvolvido com Django. O objetivo principal é permitir que usuários façam upload, visualizem, editem, excluam e filtrem suas fotos de forma intuitiva e organizada. A funcionalidade anterior de "notas" foi removida para focar exclusivamente na gestão de imagens.

🚀 Visão Geral do Projeto
O projeto oferece uma plataforma web para que cada usuário autenticado tenha sua própria galeria de fotos.

✨ Principais Funcionalidades
Autenticação de Usuários: Login, cadastro e logout de usuários.

Upload de Fotos: Envio de novas imagens com título, descrição e tags.

Galeria de Fotos: Visualização de todas as fotos do usuário em um layout responsivo.

Edição de Fotos: Alteração dos metadados (título, descrição, tags) de fotos existentes.

Exclusão de Fotos: Remoção de fotos da galeria e do armazenamento físico.

Filtro Avançado de Fotos: Página dedicada para pesquisar fotos por título, tags ou data de upload.

Favicon Personalizado: Ícone na aba do navegador para identificação do site.

Página 404 Personalizada: Mensagem amigável para URLs não encontradas.

🏛️ Arquitetura e Armazenamento de Dados
O projeto utiliza o framework web Django para o backend, com a seguinte estrutura de armazenamento:

Django Framework: Base para o desenvolvimento web, gerenciando views, URLs, modelos e templates.

Banco de Dados (SQLite): O db.sqlite3 (ou outro SGBD configurado) é utilizado para armazenar os metadados das fotos (título, descrição, tags, ID do usuário, data de upload) e o caminho relativo para o arquivo de imagem. Importante: Os dados binários das imagens (as fotos em si) não são armazenados diretamente no banco de dados.

Sistema de Arquivos: As imagens reais são salvas fisicamente no disco rígido do servidor (ou computador local em desenvolvimento). O diretório de armazenamento é configurado através de MEDIA_ROOT no settings.py.

📸 Como o Armazenamento de Imagens Funciona
O processo de upload e armazenamento de imagens é central para a aplicação e segue as melhores práticas do Django:

Campo ImageField no Modelo (usuarios/models.py):

O modelo Foto possui um campo imagem = models.ImageField(upload_to='fotos/').

ImageField é um tipo de campo específico do Django para lidar com uploads de arquivos de imagem.

upload_to='fotos/' instrui o Django a criar uma subpasta chamada fotos dentro do diretório MEDIA_ROOT e salvar os arquivos de imagem lá.

Configurações MEDIA_ROOT e MEDIA_URL (seu_projeto/settings.py):

MEDIA_ROOT: Define o caminho absoluto no sistema de arquivos onde todos os arquivos de mídia (incluindo as fotos) serão armazenados. Ex: /home/usuario/seu_projeto/media/.

MEDIA_URL: Define a URL base que será usada para acessar esses arquivos de mídia através do navegador. Ex: /media/.

Quando uma imagem é salva, o Django a coloca em MEDIA_ROOT/fotos/nome_do_arquivo.jpg, e a URL para acessá-la será MEDIA_URL/fotos/nome_do_arquivo.jpg.

Processo de Upload na View (usuarios/views.py - upload_foto):

O formulário HTML (upload_foto.html) deve ter enctype="multipart/form-data" para permitir o envio de arquivos.

Na view upload_foto, o arquivo é recebido via request.FILES.get('imagem').

Ao criar uma nova instância do modelo Foto e atribuir imagem=imagem_file, e em seguida chamar nova_foto.save(), o Django automaticamente:

Salva o arquivo de imagem no diretório MEDIA_ROOT/fotos/.

Armazena o caminho relativo do arquivo (ex: fotos/nome_do_arquivo.jpg) no campo imagem do registro no banco de dados.

Acessando a Imagem no Template (.url):

Quando um objeto Foto é recuperado do banco de dados (ex: foto_obj), o campo foto_obj.imagem é um objeto que representa o arquivo.

A propriedade .url (ex: foto_obj.imagem.url) é automaticamente gerada pelo Django para fornecer a URL completa do arquivo de imagem, permitindo que o navegador a exiba.

📦 A Biblioteca Pillow
A biblioteca Pillow (pip install Pillow) é um pré-requisito essencial para o Django quando se trabalha com ImageField. Embora não seja explicitamente chamada em cada linha do seu views.py para salvar a imagem, ela é fundamental nos bastidores para:

Processamento de Imagem: Redimensionamento, corte, conversão de formatos, etc.

Validação: Verificar se o arquivo enviado é realmente uma imagem válida.

Manipulação Interna: O ImageField do Django utiliza o Pillow para lidar com as operações de arquivo de imagem.

🛠️ Configuração e Instalação do Projeto
Siga estes passos para configurar e executar o projeto em seu ambiente local.

Pré-requisitos
Python 3.x

pip (gerenciador de pacotes do Python)

🚀 Instalação
Clone o Repositório (se aplicável):

git clone <url_do_seu_repositorio>
cd <nome_da_pasta_do_projeto>

Crie e Ative um Ambiente Virtual (Recomendado):

python -m venv venv
# No Windows:
venv\Scripts\activate
# No macOS/Linux:
source venv/bin/activate

Instale as Dependências:

pip install Django Pillow

Configurações de Mídia e Estáticos (seu_projeto/settings.py):
Abra seu_projeto/settings.py (o arquivo settings.py que está na pasta principal do seu projeto, ex: Galerize/novosite/settings.py).
Adicione ou verifique as seguintes linhas no final do arquivo:

import os # Certifique-se de que esta linha está no topo do arquivo

# Diretório base do projeto (geralmente já configurado)
# BASE_DIR = Path(__file__).resolve().parent.parent # Para Django 3.x+
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__))) # Para Django mais antigos

# Configurações de Mídia (Uploads de Usuários)
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
MEDIA_URL = '/media/'

# Configurações de Arquivos Estáticos (CSS, JS, Imagens de Tema)
STATIC_URL = '/static/'
STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static'), # Aponta para a pasta 'static' na raiz do seu projeto
]
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles') # Usado para 'collectstatic' em produção

# Configuração de Templates para encontrar templates na raiz do projeto (para 404.html)
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')], # Adicione esta linha
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

# Para a página 404 personalizada (apenas para teste em desenvolvimento)
# NÃO DEIXE DEBUG = False EM PRODUÇÃO SEM CONFIGURAÇÃO ADEQUADA DE ALLOWED_HOSTS
# DEBUG = False
# ALLOWED_HOSTS = ['127.0.0.1', 'localhost'] # Ou o IP/domínio do seu servidor

Configurações de URLs (seu_projeto/urls.py):
Abra seu_projeto/urls.py (o arquivo urls.py que está na pasta principal do seu projeto).
Adicione as importações e as linhas no final de urlpatterns para servir arquivos estáticos e de mídia em desenvolvimento:

from django.contrib import admin
from django.urls import path, include
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('usuarios.urls')), # Inclua suas URLs do app 'usuarios'
]

# Configuração para servir arquivos estáticos e de mídia em ambiente de desenvolvimento
if settings.DEBUG:
    urlpatterns += static(settings.STATIC_URL, document_root=settings.STATIC_ROOT)
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)

Crie as Pastas Necessárias:
Na raiz do seu projeto, crie as seguintes pastas se ainda não existirem:

media/

static/

templates/

Mova Arquivos Estáticos:
Certifique-se de que seu bootstrap.min.css está em static/css/ e seu favicon.ico está em static/.

Realize as Migrações do Banco de Dados:

Atenção: Se você já tinha dados e estava usando UUIDs e agora mudou para IDs numéricos auto-incrementais, você precisará excluir seus arquivos de migração em usuarios/migrations/ (exceto __init__.py) e excluir seu db.sqlite3 antes de rodar os comandos abaixo. Isso apagará todos os dados existentes.

python manage.py makemigrations usuarios
python manage.py migrate

Crie um Superusuário (se você recriou o banco de dados):

python manage.py createsuperuser

Siga as instruções para criar suas credenciais de administrador.

▶️ Executando o Projeto
Inicie o servidor de desenvolvimento Django:

python manage.py runserver

Acesse o site no seu navegador: http://127.0.0.1:8000/login/ (ou a URL que você configurou para o login).

💻 Detalhes da Implementação por Arquivo
usuarios/models.py
from django.db import models
from django.contrib.auth.models import User

class Foto(models.Model):
    # Chave primária auto-incremental (padrão do Django)
    # O campo 'id' será criado automaticamente como um AutoField.
    usuario = models.ForeignKey(User, on_delete=models.CASCADE)
    titulo = models.CharField(max_length=255)
    descricao = models.TextField(blank=True, null=True)
    imagem = models.ImageField(upload_to='fotos/') # Salva em MEDIA_ROOT/fotos/
    tags = models.CharField(max_length=255, blank=True, null=True, help_text="Separe as tags por vírgulas")
    data_upload = models.DateTimeField(auto_now_add=True) # Data e hora de criação automática

    def __str__(self):
        return self.titulo

Foto Model: Define a estrutura dos dados para cada foto, incluindo o relacionamento com o usuário (ForeignKey) e o campo ImageField para o arquivo da imagem. O ID é numérico e auto-incremental por padrão.

usuarios/views.py
Contém toda a lógica de backend para as funcionalidades da aplicação.

login(request): Autentica o usuário e redireciona para a página home em caso de sucesso.

logout(request): Desloga o usuário e redireciona para a página de login.

cadastro(request): Cria um novo usuário e, em caso de sucesso, loga-o e redireciona para home.

home(request): Se o usuário estiver autenticado, renderiza o template home.html (com os cards de navegação). Caso contrário, redireciona para login.

upload_foto(request):

Processa o formulário de upload.

Recebe o arquivo de imagem via request.FILES.

Formata as tags (adicionando # e juntando-as em uma string).

Cria e salva uma nova instância do modelo Foto no banco de dados, o que também salva o arquivo de imagem no MEDIA_ROOT.

galeria(request):

Recupera todas as fotos do usuário logado, ordenadas pela data de upload mais recente.

Implementa uma busca básica por título/descrição e tags (usando Q objects para consultas complexas).

Processa as tags para exibição no template (lista para iteração, string para exibição simples).

Passa o foto_obj.imagem.url para o template para exibição da imagem.

editar_foto_verificacao(request, id_foto):

Recupera uma foto específica pelo id_foto e verifica se pertence ao usuário logado.

Prepara a string de tags para preencher o campo de input no formulário de edição (removendo #).

Renderiza editar_foto.html, passando o objeto foto completo.

editar_foto(request, id_foto):

Processa a submissão do formulário de edição.

Atualiza o título, descrição e tags da foto no banco de dados.

Redireciona para a galeria.

excluir_foto_verificacao(request, id_foto):

Recupera uma foto específica e verifica a permissão.

Renderiza excluir_foto.html para confirmação da exclusão.

excluir_foto(request, id_foto):

Processa a confirmação de exclusão.

Deleta o objeto Foto do banco de dados, o que também aciona a exclusão do arquivo físico associado ao ImageField.

filtrar_fotos(request):

Nova view dedicada à funcionalidade de filtro avançado.

Recupera as 15 fotos mais recentes por padrão se nenhum filtro for aplicado.

Permite filtrar por titulo, tags ou data (usando Q objects para combinar filtros).

Renderiza filtrar_fotos.html com os resultados.

usuarios/urls.py
Define os mapeamentos de URL para as views do aplicativo usuarios.

from django.urls import path
from . import views

urlpatterns = [
    path('login/', views.login, name = 'login'),
    path('cadastro/', views.cadastro, name = 'cadastro'),
    path('home/', views.home, name = 'home'),
    path('logout/', views.logout, name = 'logout'),
    
    path('upload_foto/', views.upload_foto, name='upload_foto'),
    path('galeria/', views.galeria, name='galeria'),
    path('editar_foto_verificacao/<int:id_foto>/', views.editar_foto_verificacao, name='editar_foto_verificacao'),
    path('editar_foto/<int:id_foto>/', views.editar_foto, name='editar_foto'),
    path('excluir_foto_verificacao/<int:id_foto>/', views.excluir_foto_verificacao, name='excluir_foto_verificacao'),
    path('excluir_foto/<int:id_foto>/', views.excluir_foto, name='excluir_foto'),
    path('filtrar_fotos/', views.filtrar_fotos, name='filtrar_fotos'),
]

Templates HTML (usuarios/templates/usuarios/)
Os templates são responsáveis pela interface do usuário e usam Bootstrap para estilização responsiva.

base.html:

Template base que define a estrutura comum (cabeçalho, navbar, footer, inclusão de Bootstrap).

Inclui links de navegação para home, upload_foto, galeria, filtrar_fotos e logout.

Define o favicon do site.

Contém o bloco {% block conteudo %} onde o conteúdo específico de cada página é inserido.

Possui cards de navegação na página inicial para "Upload de Fotos", "Galeria de Fotos" e "Filtrar Fotos".

login.html: Formulário de login com campos para e-mail e senha, e um botão para ir ao cadastro.

cadastro.html: Formulário de cadastro com campos para nome, e-mail e senha, e um botão para ir ao login.

upload_foto.html:

Formulário para enviar novas fotos.

Possui enctype="multipart/form-data" para permitir o upload de arquivos.

Inclui um script JavaScript para exibir uma prévia da imagem selecionada antes do upload.

galeria.html:

Exibe as fotos do usuário em um layout de grade responsivo (cards).

Cada card de foto mostra título, descrição, tags e data de upload.

Botões "Editar" e "Excluir" em cada card, que levam às respectivas páginas de gerenciamento.

Seção para tags clicáveis que filtram a galeria.

editar_foto.html:

Formulário para editar os metadados de uma foto.

Exibe a imagem da foto atual usando {{ foto.imagem.url }} para que o usuário saiba qual foto está editando, mas não permite a troca da imagem.

Campos pré-preenchidos com os dados existentes.

excluir_foto.html:

Página de confirmação para excluir uma foto.

Exibe a imagem da foto usando {{ foto.imagem.url }} para confirmação visual.

Botões "Sim, Excluir Foto" e "Não, Voltar".

filtrar_fotos.html:

Página dedicada para pesquisar fotos.

Apresenta os 15 resultados mais recentes por padrão.

Possui campos de input separados para filtrar por título, tags e data (com seletor de calendário).

Exibe os resultados em um formato de lista vertical, com imagem, título, tags, data e botões de "Editar" e "Apagar" para cada item.

templates/404.html (Página de Erro Personalizada)
Localizado na pasta templates/ na raiz do projeto.

Exibido pelo Django quando uma URL não é encontrada (DEBUG = False).

Inclui um botão para retornar à página inicial.

✅ Testes e Verificações Finais
Após qualquer alteração, especialmente em settings.py, urls.py, models.py ou views.py, siga estes passos:

Reinicie o servidor Django: python manage.py runserver

Limpe o cache do navegador: Pressione Ctrl + Shift + R (ou Cmd + Shift + R no Mac) em suas páginas.

Teste o fluxo completo: Login, cadastro, upload de novas fotos, visualização na galeria, edição, exclusão e uso do filtro.

Verifique a pasta media/fotos/: Após o upload, confirme se o arquivo da imagem está fisicamente lá.

Verifique o painel /admin: Confirme se os registros das fotos estão corretos e se o campo "Imagem" está preenchido e é clicável.

Com esta documentação abrangente, você tem um guia completo para entender e gerenciar seu projeto de Galeria de Fotos Pessoal.